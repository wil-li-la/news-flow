{
  "name": "newsflow-lambda",
  "version": "1.0.0",
  "description": "Lambda functions for NewsFlow",
  "main": "newsApi.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-bedrock-runtime": "^3.0.0"
  }
}